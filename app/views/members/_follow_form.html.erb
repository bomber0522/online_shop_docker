<% unless current_member?(@member) %>
  <div id="follow_form">
  <% if current_member.following?(@member) %>
    <%= render 'unfollow' %><br>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>
